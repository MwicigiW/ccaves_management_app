import 'package:flutter/material.dart';

class MessengingSection extends StatelessWidget {
  const MessengingSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 175,
      decoration: BoxDecoration(
        color: Colors.white,
      ),
    );
  }
}